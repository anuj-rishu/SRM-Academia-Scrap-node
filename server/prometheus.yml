global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "srm-academia"
    static_configs:
      - targets: ["srm-app:9000"]
    metrics_path: "/metrics"
